// WarhammerItemSheet: removed.
// The original item sheet implementation caused duplicate registrations and console errors.
// This file intentionally contains no exports or registrations to avoid interfering with
// the system. If you need a custom ItemSheet, add it in a single place (e.g. in warhammer2e.js)
// or in a dedicated module file, and ensure `system.json` imports only the desired module.